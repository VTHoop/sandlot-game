/** Outs that end a half-inning, after which no run on the play counts (rules §2).
 * A shared rules constant for the run-suppression checks in the sub-resolutions. */
export const OUTS_PER_INNING = 3
