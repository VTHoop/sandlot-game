# Dependencies / build
node_modules/
dist/
build/
coverage/
dev-dist/
.convex/

# Playwright
test-results/
playwright-report/

# Env & secrets
.env
.env.*
!.env.example

# OS / editor
.DS_Store
*.log

# Agent auto-memory (lives under ~/.claude; never commit if it leaks into the repo)
memory/

# === SAN-12: local parity reference and capture script (gitignored per ADR-0006) ===
packages/engine/reference/
packages/engine/scripts/

# === Cardinal: never commit (see docs/adr/0006-ip-branding-and-data-sourcing.md) ===
# The reverse-engineered source calculator / engine spec (IP — keep private/local):
bbtn-engine-spec.md
**/*engine-spec*
# Real MLB player data (licensing — fetched at runtime/build into ignored storage):
**/mlb-data/
**/*.mlb.json
*.player-data.json
